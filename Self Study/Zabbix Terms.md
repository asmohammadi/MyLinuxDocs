# Zabbix Terms:

`Frontend`
`Dashboard`
`Widget`
`Host`
`Host Group`
`Item`
`Value Preprocessing` : Filter Logs/Data before saving to Database
`Trigger`
`Template`
`Template Group`
`Event`
`Event Tag`
`Event Correlation` : Set dependencies
`Problem`
`Action`
`Escalation` : Hierarchy of actions. Send message, email, ..., Restart service, Run script.
`Media` : Notifications(SMS, Email)
`Notification` : Template of a message
`Remote command`
`Web Scenario` : Monitoring a web URL
`Agent auto registration` : Adding host automatically to Zabbix monitoring after installing & configuring agent on Host.
`Network Discovery`
`Low-Level Discovery` : (Filesystem, Network Interface)


Host:
`Mass Update` : Add a value(Tag) for Multiple Hosts
`Hostname`
`Template`
`Group`
`Interface` : Agent, SNMP, JMX, IPM
`Tag`
`macro` : MySQL data
`Inventory` : Disabled, Manual, Auto
`Encryption`
`Value Mapping`

